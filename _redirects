# Force HTTPS Redirects for Netlify / Cloudflare Pages
http://* https://:splat 301!

# Admin portal clean routing
/admin /admin.html 200
/admin/* /admin.html 200

# SPA fallback routing for clean URLs
/* /index.html 200
